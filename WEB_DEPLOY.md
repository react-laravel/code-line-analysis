# Web Deployment

桌面 **Tauri** 版在 `main` 分支。可部署的静态网页版与 Electron 完整实现保留在 [`electron`](https://github.com/react-laravel/code-line-analysis/tree/electron) 分支。

Self-hosted 部署 workflow（`.github/workflows/deploy-self-hosted.yml`）在推送 **`electron`** 分支时触发。请参阅该分支上的 `WEB_DEPLOY.md` / `deploy.php`。
